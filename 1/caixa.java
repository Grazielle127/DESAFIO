public class caixa{
    void calculavalorfinal() {
        return (this.Double >= valor) ? this.produto *= valorASerSacado : "desconto de 10%" ;  
    }
    
}
public class main
<table>
    <c:forEach items="${produtos}" var="p" varStatus="contador">
        <tr>
            <td> ${energetico } valor=5,49</td>
            <td> ${arroz } valor=20,00</td>
            <td> ${chocolate } valor=4,50</td>
            <td> ${leite } valor=3,73</td>
            <td> ${abacaxi } valor=2,40</td>
        <c:if test="${contador.count % 3}">
        </tr>
        <tr>
        </c:if>
    </c:forEach>
</table>
